<?xml version="1.0" encoding="utf-8" ?>
<views:ViewBase x:TypeArguments="viewModels:MainViewModel"
                xmlns="http://xamarin.com/schemas/2014/forms"
                xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:d="http://xamarin.com/schemas/2014/forms/design"
                xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
                xmlns:viewModels="clr-namespace:Template.ViewModels;assembly=Template"
                xmlns:views="clr-namespace:Template.Views"
                prism:ViewModelLocator.AutowireViewModel="True"
                x:DataType="viewModels:MainViewModel"
                mc:Ignorable="d"
                x:Class="Template.Views.MainView">

    <StackLayout>
        <!-- Place new controls here -->
        <Label HorizontalOptions="Center"
           VerticalOptions="CenterAndExpand">
            <Label.FormattedText>
                <FormattedString>
                    <Span Text="{Binding MyText}"/>
                    <Span Text=" to "/>
                    <Span Text="{Binding [AppTitle]}"/>
                </FormattedString>
            </Label.FormattedText>
        </Label>
    </StackLayout>

</views:ViewBase>
